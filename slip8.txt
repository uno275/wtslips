Q. 1) Write a JavaScript to display message ‘Exams are near, have you started preparing for?’ (usealertbox ) and Accept any two numbers from user and display addition of two
number .(Use Prompt andconfirm box)

alert('Exams are near, have you started preparing for?');
// Accept two numbers from user
var num1 = prompt('Enter first number:');
var num2 = prompt('Enter second number:');
// Convert input to numbers
num1 = Number(num1);
num2 = Number(num2);
// Display addition of two numbers using confirm box
var result = num1 + num2;
confirm('The addition of ' + num1 + ' and ' + num2 + ' is ' + result);